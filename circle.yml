machine:
  services:
    - docker
  environment:
    KUBE_DEPLOY_DIR: ./k8s
    GIT_SHA: ${CIRCLE_SHA1}
    APP_NAME: $(tr [A-Z] [a-z] <<< ${CIRCLE_PROJECT_REPONAME:0:15} | tr '_ ' '-')

checkout:
 post:
   - git submodule sync
   - git submodule update --init # use submodules

dependencies:
  cache_directories:
    - "~/.kube"
    - "~/.docker"
  override:
    - docker login -u ${DOCKER_USER} -p ${DOCKER_PASS} -e ${DOCKER_EMAIL}
    - sudo apt-get install -y wget
    - sudo apt-get install -y gettext
    - wget https://storage.googleapis.com/kubernetes-release/release/v1.4.6/bin/linux/amd64/kubectl -O ~/.kube/kubectl
    - chmod +x ~/.kube/kubectl
    - echo ${KUBECONFIGB64} | base64 -d > ~/.kube/config
    - if [[ -e ~/.docker/image.tar ]]; then docker load -i ~/.docker/image.tar; fi
    - docker build --rm=false -t ${DOCKER_USER}/${APP_NAME} .
    - mkdir -p ~/.docker; docker save ${DOCKER_USER}/${APP_NAME} > ~/.docker/image.tar
    - docker tag -f ${DOCKER_USER}/${APP_NAME}:latest ${DOCKER_USER}/${APP_NAME}:git-${GIT_SHA}

test:
  override:
    - echo "test"

deployment:
  production:
    branch: master
    commands:
       - docker push ${DOCKER_USER}/${APP_NAME}:git-${GIT_SHA}
       - docker push ${DOCKER_USER}/${APP_NAME}:latest
       - mkdir build
       - for f in ${KUBE_DEPLOY_DIR}/*.y*ml; do envsubst < $f > build/$(basename $f); done
       - ~/.kube/kubectl apply -f build/
       - ~/.kube/kubectl rollout status deployment/${APP_NAME}
       - echo "Image ${DOCKER_USER}/${APP_NAME}:git-${GIT_SHA} deployed to https://${APP_NAME}.${BASE_APP_HOST}/"

